const pageStyles = {
    display: "flex",
    height: "80%",
    alignItems: "center",
    justifyContent: "center",
    flexDirection: "column",
    textAlign: "center"
}

const registerTitle = {
    fontWeight: "bold",
    fontSize: "27px",
    marginBottom: "1em"
}

const errorsText = {
    textAlign: "center",
    margin: "2em",
    color: "#FF4040"
}

const errorsDisplay = {
    textAlign: "center",
    margin: "1em",
    color: "#FF4040"
}

export {pageStyles, registerTitle, errorsDisplay, errorsText};